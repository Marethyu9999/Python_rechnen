import asyncio

async def input():
    # Eingabe der Variablen a, b, c
    # input of variables a,b,c
    a = float(input("a: "))
    b = float(input("b: "))
    c = float(input("c: "))
    return a, b, c